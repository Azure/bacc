name: az-deploy-matrix
on:
  workflow_dispatch:
    inputs:
      location:
        description: 'Deployment location'
        required: false
        default: 'eastus'
        type: string
jobs:
  deploy:
    strategy:
      max-parallel: 1
      fail-fast: true
      matrix:
        enable_application_containers: [false, true]
        enable_application_packages: [false, true]
    uses: ./.github/workflows/az-deploy.yaml
    with:
      location: ${{ github.event.inputs.location }}
      prefix: batch-starter
      suffix_salt: ${{ github.run_id }} ${{ matrix.enable_application_containers }}
      enable_application_containers: ${{ matrix.enable_application_containers }}
      enable_application_packages: ${{ matrix.enable_application_packages }}
      deploy: true
      cleanup: true
    secrets: inherit
